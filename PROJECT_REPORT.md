# تقرير شامل: شبكة صِوار الزراعية الذكية
## من بداية الإنشاء حتى المرحلة الحالية

---

## 1. نظرة عامة على المشروع

**اسم المشروع:** شبكة صِوار الزراعية الذكية (Siwar Agricultural Network)

**الوصف:** منصة رقمية تربط بين القطاعات الزراعية المختلفة (النخيل، المشاتل، المزادات، الثروة الحيوانية، المحاصيل، وغيرها) في شبكة واحدة ذكية. تتيح المنصة للمستخدمين تصفح الأنشطة الزراعية (فرص بيع/شراء، مزادات، منتجات، خدمات، فعاليات) حسب القطاع والقطاع الفرعي، مع إمكانية البحث والتصفية.

**الجمهور المستهدف:** المزارعون، أصحاب المزارع، المستثمرون الزراعيون، تجار المزادات، وشركات الخدمات الزراعية في المملكة العربية السعودية والمنطقة العربية.

**اللغة والاتجاه:** عربية بالكامل، اتجاه RTL (من اليمين إلى اليسار).

---

## 2. التقنيات المستخدمة

| التقنية | الإصدار | الاستخدام |
|---------|---------|-----------|
| React | 18.3.1 | إطار العمل الأمامي |
| TypeScript | 5.5.3 | لغة البرمجة |
| Vite | 5.4.2 | أداة البناء والتطوير |
| Tailwind CSS | 3.4.1 | التصميم والتنسيق |
| Supabase | 2.57.4 | قاعدة البيانات والمصادقة |
| Lucide React | 0.446.0 | الأيقونات |
| خط Tajawal | - | الخط العربي الرئيسي |

---

## 3. البنية المعمارية

### 3.1 الواجهة الأمامية (Frontend)

الصفحة الرئيسية هي صفحة واحدة ديناميكية (Single Page Application) تعتمد بالكامل على بيانات قاعدة البيانات. لا توجد صفحات ثابتة — كل المحتوى يُحمّل من Supabase.

**المسار البرمجي:**
```
src/
├── App.tsx                          # الصفحة الرئيسية (النقطة المركزية)
├── main.tsx                         # نقطة الدخول
├── index.css                        # الأنماط العامة + التأثيرات ثلاثية الأبعاد
├── lib/
│   └── supabase.ts                  # اتصال Supabase
├── services/
│   └── domainService.ts             # طبقة الوصول للبيانات (Data Access Layer)
├── types/
│   └── index.ts                     # تعريفات الأنواع (TypeScript Types)
└── components/
    └── network-home/                # جميع مكونات الواجهة
        ├── NetworkHeader.tsx        # الرأس (شعار + بحث + مدينة + حساب)
        ├── MainSectorSlider.tsx     # شريط القطاعات الرئيسية (نخيل، مشاتل، مزادات)
        ├── SectorIcons.tsx          # رسومات SVG للأيقونات (نخلة، تفاح، مطرقة)
        ├── SubSectorSlider.tsx      # شريط القطاعات الفرعية + الفروع
        ├── SectorFilterBar.tsx      # شرائح التصفية والفرز
        ├── SectorSearch.tsx         # البحث داخل القطاع
        ├── NetworkPulseHeader.tsx  # رأس قسم "نبض الشبكة"
        ├── NetworkPulseList.tsx     # قائمة بطاقات الأنشطة
        ├── NetworkPulseCard.tsx     # بطاقة نشاط واحدة
        ├── NetworkPulseSkeleton.tsx # هيكل تحميل (Skeleton)
        ├── SectorSliderSkeleton.tsx # هيكل تحميل للقطاعات
        ├── SubSectorSliderSkeleton.tsx # هيكل تحميل للقطاعات الفرعية
        ├── CreationPanelSkeleton.tsx   # هيكل تحميل للوحة الإنشاء
        ├── SectorCreationPanel.tsx # لوحة إنشاء نشاط جديد
        ├── CreationModal.tsx       # نافذة منبثقة لاختيار نوع النشاط
        ├── LoginPrompt.tsx         # مطالبة بتسجيل الدخول
        ├── EmptyState.tsx          # حالة فارغة (لا توجد أنشطة)
        ├── BottomNavigation.tsx    # شريط التنقل السفلي
        └── Toast.tsx               # رسائل التنبيه المنبثقة
```

### 3.2 قاعدة البيانات (Supabase / PostgreSQL)

**المسار:** `supabase/migrations/`

تم إنشاء 3 هجرات (Migrations):

#### الهجرة الأولى: `create_siwar_network_schema`
أنشأت المخطط الكامل:

| الجدول | الوصف | الأعمدة الرئيسية |
|--------|-------|-----------------|
| `sectors` | القطاعات الرئيسية | name, slug, description, icon, image, display_order, is_active, is_featured, search_placeholder, available_actions (jsonb), filter_configuration (jsonb) |
| `sub_sectors` | القطاعات الفرعية | sector_id (FK), name, slug, icon, display_order, is_active |
| `companies` | الشركات | name, logo, is_verified, description, city |
| `opportunities` | الفرص (بيع/شراء/استثمار/خدمة) | sector_id, sub_sector_id, company_id, type, title, description, image, quantity, quality, city, price, status |
| `auctions` | المزادات | sector_id, source_sector_id, source_sub_sector_id, company_id, title, description, image, current_price, city, status (live/upcoming), start_time, end_time |
| `products` | المنتجات | sector_id, sub_sector_id, company_id, name, description, image, price, city |
| `services` | الخدمات | sector_id, sub_sector_id, company_id, name, description, image, price, city |
| `events` | الفعاليات | sector_id, company_id, title, description, image, city, event_date |

**عرض موحد `network_pulse`:** يجمع كل الأنشطة (فرص + مزادات + منتجات + خدمات + فعاليات) في جدول واحد افتراضي باستخدام `UNION ALL`، مع خاصية `security_invoker = true` لضمان تطبيق سياسات RLS على الجداول الأصلية.

#### الهجرة الثانية: `fix_network_pulse_auctions_both_sectors`
حلت مشكلة ظهور المزادات في قطاع واحد فقط. الآن كل مزاد يظهر في **قطاع المزادات** و**القطاع المصدر** (مثلاً مزاد على نخيل يظهر في قطاع النخيل وقطاع المزادات).

#### الهجرة الثالثة: `add_parent_id_to_sub_sectors`
أضافت عمود `parent_id` لجدول `sub_sectors` لدعم التفرعات (القطاع الفرعي يمكن أن يكون له فروع فرعية). تمت إضافة بيانات تجريبية لبذور عضوية/تقليدية، أبقار حلوب/لحم، دواجن بيض/لحم.

### 3.3 الأمان (Row Level Security)

- **مفعّل على كل الجداول** بدون استثناء.
- **سياسات القراءة:** متاحة للجميع (anon + authenticated) — التصفح عام بدون تسجيل دخول.
- **لا توجد سياسات الكتابة بعد** — التطبيق يعرض مطالبة بتسجيل الدخول عند محاولة الإنشاء.

---

## 4. تصميم الواجهة (UI/UX)

### 4.1 الهوية البصرية

- **الألوان الرئيسية:** أخضر زمردي/فيروزي (teal #0d9488) كلون أساسي، مع تدرجات زراعية طبيعية.
- **الخلفية:** تدرج شبكي خفيف (mesh gradient) بألوان فيروزية وزرقاء وبرتقالية شفافة + نقش حبيبي خفيف.
- **الخط:** Tajawal (عربي حديث) بأوزان 400/500/700/900.
- **التصميم ثلاثي الأبعاد:** تأثيرات neumorphic (نيومورفيزم) على الأيقونات والأزرار والبطاقات مع ظلال داخلية وخارجية.
- **الزجاجية (Glassmorphism):** تأثيرات زجاجية ضبابية على الرأس وبعض العناصر.

### 4.2 الرسومات (SVG Illustrations)

بدلاً من الصور الطبيعية، تم استخدام رسومات SVG مرسومة يدوياً داخل أيقونات القطاعات:
- **النخيل:** رسمة نخلة بأوراق خضراء وتمور برتقالية متدلية.
- **المشاتل:** رسمة شجرة تفاح بتفاح أحمر وأوراق خضراء.
- **المزادات:** رسمة مطرقة مزادات خشبية مع قاعدة الضرب وخطوط الصوت.

### 4.3 التخطيط العام للصفحة

```
┌─────────────────────────────────────┐
│  الرأس: شعار + بحث + مدينة + حساب   │
├─────────────────────────────────────┤
│  شريط القطاعات الرئيسية (أفقية)      │
│  [نخيل] [مشاتل] [مزادات] [...]      │
├─────────────────────────────────────┤
│  شريط القطاعات الفرعية (أفقية)      │
│  [بذور] [شتلات] [أسمدة] [...]       │
│  + الفروع الفرعية (إن وجدت)         │
├─────────────────────────────────────┤
│  شرائح التصفية + الفرز              │
│  [المدينة ▼] [الجودة ▼] [الأحدث ▼]  │
├─────────────────────────────────────┤
│  نبض الشبكة (رأس + عداد الأنشطة)    │
├─────────────────────────────────────┤
│  بطاقات الأنشطة (قائمة عمودية)      │
│  ┌─────────────────────────────┐    │
│  │ بطاقة نشاط (صورة + عنوان)   │    │
│  │ (شركة + مدينة + وقت)        │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │ بطاقة نشاط ...              │    │
│  └─────────────────────────────┘    │
├─────────────────────────────────────┤
│  [الرئيسية] [الشبكة] [+] [إشعار] [حساب] │
└─────────────────────────────────────┘
```

### 4.4 التفاعلات والحركات (Animations & Micro-interactions)

- **ظهور مرن (Spring bounce):** القطاعات تظهر بتأثير ارتدادي عند التحميل.
- **تكبير الأيقونات:** الرسومات تتكبر قليلاً عند المرور عليها (hover).
- **تأثير الضغط (Tap scale):** الأزرار تتقلص قليلاً عند الضغط.
- **هياكل التحميل (Skeletons):** تظهر أثناء تحميل البيانات مع تأثير لمعان (shimmer).
- **حلقة المباشر (Live ring):** المزادات المباشرة لها حلقة نابضة حمراء.
- **انتقالات سلسة:** كل التغييرات (تبديل القطاع، التصفية) بانتقالات CSS سلسة.
- **تدرج الأطفال (Stagger):** عناصر القائمة تظهر بالتتابع.

### 4.5 شريط التنقل السفلي

5 أزرار: الرئيسية، الشبكة، زر الإنشاء (في المنتصف، بارز)، الإشعارات (مع عداد)، الحساب.

---

## 5. منطق التطبيق (Application Logic)

### 5.1 تدفق البيانات

1. **تحميل القطاعات:** عند فتح الصفحة، تُحمّل القطاعات النشطة من `sectors` مرتبة حسب `display_order`. أول قطاع يُحدد تلقائياً كنشط.
2. **تحميل القطاعات الفرعية:** عند تغيير القطاع، تُحمّل القطاعات الفرعية المرتبطة. يتم إعادة ضبط البحث والتصفية.
3. **تحميل الفروع الفرعية:** عند اختيار قطاع فرعي له `parent_id`، تُحمّل فروعه.
4. **نبض الشبكة:** تُحمّل الأنشطة من عرض `network_pulse` المفلتر حسب:
   - القطاع النشط
   - القطاع الفرعي (إن وُجد) + فروعه
   - نص البحث (debounced 400ms)
   - التصفيات النشطة (المدينة، إلخ)
   - الفرز (الأحدث/الأقدم)
5. **عداد الأنشطة:** يُحمّل عدد الأنشطة لكل قطاع بشكل متوازي.

### 5.2 الإنشاء

عند الضغط على زر الإنشاء:
1. تظهر نافذة منبثقة تعرض الإجراءات المتاحة للقطاع النشط (من `available_actions` في `sectors`).
2. إذا لم يكن المستخدم مسجلاً، تظهر مطالبة بتسجيل الدخول.
3. (حالياً غير مكتمل — يحتاج تطوير فعلي للنماذج والمصادقة).

### 5.3 البحث والتصفية

- **البحث:** نص حر مع تأخير 400ms (debounce) يبحث في `title` و`description`.
- **التصفية:** ديناميكية من `filter_configuration` في جدول `sectors` (jsonb) — كل قطاع له تصفياته الخاصة.
- **الفرز:** الأحدث أو الأقدم.

---

## 6. الأنواع (TypeScript Types)

```typescript
Sector:      القطاع الرئيسي (id, name, slug, available_actions, filter_configuration, ...)
SubSector:   القطاع الفرعي (id, sector_id, parent_id, name, slug, icon, ...)
Company:     الشركة (id, name, logo, is_verified, city, ...)
NetworkPulseEvent: نشاط موحد (activity_type, activity_subtype, title, sector_id, ...)
SectorAction: إجراء متاح (id, label, icon)
FilterConfig: تصفية (id, label, type, options)
```

---

## 7. الحالة الحالية (ما هو مكتمل)

### مكتمل ويعمل:
- [x] تصميم الواجهة الكامل (هوية بصرية، تخطيط، تأثيرات ثلاثية الأبعاد)
- [x] شريط القطاعات الرئيسية مع رسومات SVG
- [x] شريط القطاعات الفرعية مع دعم التفرعات (3 مستويات)
- [x] البحث مع debounce
- [x] التصفية الديناميكية حسب القطاع
- [x] الفرز (الأحدث/الأقدم)
- [x] قائمة بطاقات الأنشطة (نبض الشبكة)
- [x] هياكل التحميل (Skeletons) لكل قسم
- [x] شريط التنقل السفلي
- [x] نافذة الإنشاء المنبثقة
- [x] مطالبة تسجيل الدخول
- [x] رسائل Toast
- [x] حالة فارغة (Empty state)
- [x] قاعدة البيانات الكاملة (7 جداول + عرض موحد)
- [x] سياسات RLS للقراءة العامة
- [x] دعم RTL كامل
- [x] تصميم متجاوب (موبايل + ديسكتوب)

### غير مكتمل / يحتاج تطوير:
- [ ] نظام المصادقة (تسجيل دخول/تسجيل حساب) — Supabase Auth
- [ ] سياسات الكتابة (INSERT/UPDATE/DELETE) في RLS
- [ ] نماذج إنشاء المحتوى الفعلية (فرص، مزادات، منتجات، خدمات، فعاليات)
- [ ] صفحة تفاصيل النشاط (عند الضغط على بطاقة)
- [ ] صفحة ملف الشركة
- [ ] نظام الإشعارات الفعلي
- [ ] نظام المزادات المباشر (bidding في الوقت الفعلي)
- [ ] صفحة الحساب الشخصي
- [ ] صفحة استكشاف الشبكة
- [ ] العداد التنازلي للمزادات (time_remaining)
- [ ] رفع الصور (Supabase Storage)
- [ ] التحقق من الشركات (verification badge system)
- [ ] نظام التفضيلات/المحفوظات
- [ ] الإشعارات الفورية (Realtime subscriptions)
- [ ] نظام التقييمات والمراجعات
- [ ] خريطة المواقع الزراعية
- [ ] لوحة تحكم للشركات
- [ ] لوحة تحكم للمشرفين (Admin panel)

---

## 8. تفاصيل التصميم الفني (CSS)

ملف `index.css` يحتوي على نظام تصميم متكامل:

| الفئة (Class) | الاستخدام |
|---------------|----------|
| `mesh-bg` | خلفية بتدرج شبكي |
| `bg-grain` | نقش حبيبي خفيف |
| `glass` | تأثير زجاجي شفاف |
| `glass-agri` | زجاجي فيروزي زراعي |
| `glass-dark` | زجاجي داكن |
| `icon-3d` | أيقونة نيومورفية ثلاثية الأبعاد |
| `icon-3d-raised` | أيقونة دائرية بارزة |
| `icon-emboss` | تأثير نقش بارز |
| `chip-3d` / `chip-3d-active` | شرائح نيومورفية |
| `chip-branch` / `chip-branch-active` | شرائح الفروع الفرعية |
| `card-prominent` | بطاقة بارزة بظلال فيروزية |
| `header-icon-3d` | زر رأس نيومورفي |
| `badge-3d` | شارة دائرية ثلاثية الأبعاد |
| `spring-in` | ظهور مرن |
| `stagger` | ظهور متتابع للأطفال |
| `live-ring` | حلقة نابضة للمزادات المباشرة |
| `skeleton-shimmer` | لمعان هيكل التحميل |
| `gradient-border` | حد متدرج متحرك |
| `card-3d` | بطاقة بإمالة ثلاثية الأبعاد |
| `tap-scale` | تأثير الضغط |
| `no-scrollbar` | إخفاء شريط التمرير |
| `touch-scroll` | تمرير لمسي سلس |

---

## 9. ملاحظات للمطور القادم (ChatGPT)

1. **اللغة:** التطبيق عربي بالكامل (RTL). أي نص جديد يجب أن يكون بالعربية.
2. **قاعدة البيانات أولاً:** كل المحتوى ديناميكي من Supabase. لا توجد بيانات ثابتة في الكود.
3. **التصميم النيومورفي:** النمط البصري يعتمد على Neumorphism + Glassmorphism. حافظ على هذا النمط في أي مكونات جديدة.
4. **الأنواع:** استخدم الأنواع المعرّفة في `src/types/index.ts` — لا تستخدم `any`.
5. **طبقة الخدمة:** كل الوصول للبيانات يمر عبر `src/services/domainService.ts`. أضف دوال جديدة هناك.
6. **الأيقونات:** استخدم `lucide-react` للأيقونات العامة، و `SectorIcons.tsx` للرسومات المخصصة.
7. **المسار:** استخدم `@/` كاختصار (يُشير إلى `src/`).
8. **عدم استخدام الصور الطبيعية:** استخدم رسومات SVG أو أيقونات، وليس صوراً طبيعية.
9. **الاستجابة:** التطبيق مصمم للموبايل أولاً (Mobile-first) مع دعم الديسكتوب.
10. **الأمان:** RLS مفعّل. أي جدول جديد يجب أن يفعّل عليه RLS مع سياسات مناسبة.

---

## 10. ملخص للنقل إلى ChatGPT

> "لدي تطبيق React + TypeScript + Vite + Supabase اسمه 'شبكة صِوار الزراعية الذكية'. هو منصة عربية (RTL) تربط القطاعات الزراعية (نخيل، مشاتل، مزادات) في شبكة واحدة. قاعدة البيانات تحتوي على 7 جداول (sectors, sub_sectors, companies, opportunities, auctions, products, services, events) مع عرض موحد network_pulse. الواجهة تستخدم Tailwind CSS بتصميم نيومورفي + زجاجي، خط Tajawal، رسومات SVG مخصصة. المكتمل: تصميم الواجهة، شريط القطاعات والقطاعات الفرعية، البحث والتصفية، بطاقات الأنشطة، هياكل التحميل، شريط التنقل السفلي. غير المكتمل: المصادقة، نماذج الإنشاء، صفحات التفاصيل، المزادات المباشرة، الإشعارات، رفع الصور، لوحات التحكم."

---

*تاريخ التقرير: 4 أغسطس 2026*
*حالة المشروع: قيد التطوير — المرحلة الأولى (الواجهة + قاعدة البيانات) مكتملة*
